// aiClients/copilot.js
// Placeholder for GitHub Copilot integration (API not public for completions)

/**
 * @param {string} prompt
 * @param {object} options
 * @returns {Promise<string>}
 */
async function ask(prompt, options = {}) {
  // GitHub Copilot does not offer a public chat/completion API as of now
  // Prepare for future integration
  return '[Copilot integration not implemented. Awaiting public API.]';
}

module.exports = { ask };
