// Core Game Engine
// Fundamental mechanics and state management for SafeSoundArena.

export function calculateReputation(points: number, multiplier: number): number {
  return Math.floor(points * multiplier);
}

// Add more core mechanics here as needed.
