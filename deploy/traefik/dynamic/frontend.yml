http:
  routers:
    frontend:
      rule: Host(`app.example.com`)
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      service: frontend
  services:
    frontend:
      loadBalancer:
        servers:
          - url: 'http://127.0.0.1:3000'
