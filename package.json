{
  "name": "safesoundarena-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "safesoundarena-server",
    "frontend"
  ],
  "scripts": {
    "test": "mocha --exit",
    "dev": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "sinon": "^20.0.0"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.14.1"
  }
}
