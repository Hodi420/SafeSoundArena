{
  "name": "safesoundarena-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "mocha --exit"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "sinon": "^20.0.0"
  }
}
